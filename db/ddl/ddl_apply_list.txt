# ddl_apply_list.txt
# --- 이 파일에 적힌 SQL 파일만 순서대로 적용됨 ---
# --- 주석은 # 으로 시작 ---
# 새로 추가한 sql은 가장 아래에 추가
# 파일 이름을 중간에 지우면 순서가 꼬임
# 같은 SQL을 두 번 넣으면 두 번 적용 (DDL은 대부분 재적용 불가이므로 주의)
# 한 줄 = 한 SQL 파일명
# 순서대로 적용됨
# 마지막에 줄바꿈 해줄까?



VS20251115_01_create_db_tables.sql
